local active = true
if (not active) then return end

-- Test
game:GetService("Players").PlayerAdded:Connect(function(plr)
  repeat task.wait()
  until (plr:IsDescendantOf(game:GetService("Players")))

  plr:SetAttribute("123", 123)
end)

--[[ . . . ]]
